"""Shared CLI daemon interface placeholder.

Phase 1 (Android): use Termux + spotdl directly.
Phase 2 (Desktop): run this daemon for queue + progress parsing.
"""

if __name__ == "__main__":
    print("Daemon placeholder")
