import '../platform_bridge/command_executor.dart';

class AndroidTermuxAdapter {
  final CommandExecutor executor;

  AndroidTermuxAdapter(this.executor);

  // TODO: Implement Termux intent bridge and JSON queue interface.
}
